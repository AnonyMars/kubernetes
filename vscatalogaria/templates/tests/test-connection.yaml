apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vscatalogaria.fullname" . }}-test-connection"
  labels:
    {{- include "vscatalogaria.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vscatalogaria.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
